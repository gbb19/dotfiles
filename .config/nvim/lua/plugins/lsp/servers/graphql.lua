-- lua/plugins/lsp/servers/graphql.lua
-- Overrides for GraphQL LSP server.

return {
  -- GraphQL LSP automatically searches for configuration files (.graphqlrc, graphql.config.json, etc.)
  -- to scan schemas across different domain and module files.
  -- Add any custom settings or overrides here if needed.
}
